# 视觉与交互规范

> 当前设计约定 · 2026-09-07

Earshot 是录音和阅读工具。视觉层级服务于正文、录制状态和当前动作，避免多层卡片和装饰性仪表盘。交互行为见 [docs/interaction.md](docs/interaction.md)。

## 颜色与文字

应用跟随 macOS 明暗模式，运行中的颜色 token 以 `src/renderer/styles.css` 为准。Nocturne 深蓝紫是底色，品牌紫用于选择、焦点和开始；红色用于录音与停止。说话人采用来源色，不能与录音状态混用。

系统字体栈优先 `-apple-system`、`BlinkMacSystemFont`、`SF Pro Text`、`PingFang SC`。层级靠字号、字重和留白；时钟、时长用等宽数字。正文需要舒适行距和受控阅读宽度，小字对比度目标至少 4.5:1。

## 布局

主窗口包含侧栏会库与正文区。设置保持居中，标签和控件成行对齐；窗口变小时纵向滚动，核心操作不依赖水平滚动。录制、回听控制跨页面持续可达。

录中浮窗和语音输入浮窗都是 Electron 窗口。语音输入浮窗按状态明确显示收音、处理、结果与错误，避免把实现术语放进用户流程。

## 状态与文案

错误要说明当前发生什么，并提供对应动作。已保存、已配置、已授权、已连接和已验证是不同状态，不得混写。危险操作提供合理撤销；开始录制、写入文字和删除都需要清楚反馈。

键盘焦点必须可见。快捷键录入、下拉框和菜单使用可访问语义；不能只凭颜色区分来源或失败。渲染文案默认中文，新增文案沿用同一术语。

## 图标

应用图标为 `assets/earshot-icon.png`，由生成图像选用并导出为 `assets/Earshot.icns`。运行 `node scripts/build-icon.mjs` 可从 PNG 重新生成 macOS 图标。仓库只保留实际使用的资源。

视觉验收以实际 React 界面为准，开发预览中的演示状态不能证明真机采集或跨应用输入。
