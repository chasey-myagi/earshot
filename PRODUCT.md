# 产品范围

> 当前产品约定 · 2026-09-09

Earshot 面向 Apple Silicon Mac 上需要留存会议原声、回看转录和日常语音输入的用户。界面不要求用户理解进程、模型运行时或插件协议。

## 录会

1. 麦克风与系统音频同时录制，原始 WAV 保存在本机。录音优先于云处理，转写断线不应停止落盘。
2. 开始后显示录中浮窗；会库随时打开本场完整稿，关闭窗口不等于停止录音。阅读历史段落时不强制滚到最新。
3. 实时转写是现场稿；停录后自动执行文件转写，派生稿可重跑。重连只恢复后续实时音频，不承诺补齐断线文字。
4. 自动区分说话人的选项在开录时固定本场值。用户起名有短暂撤销期，撤销期结束后才登记人物及声纹。
5. 本机声纹尝试在后续会话套用已记住的名字；失败不影响起名、读稿或原音。没有独立人物管理页或声纹资格门。
6. 支持会话改名、按时间戳回听、0.75–2 倍速、前后 5 秒、TXT / JSON 导出、删除与撤销。录中或回听时可以添加带名称的时间标记。
7. 停录后可修改单段正文与说话人、撤销上次修改或恢复原稿。修改保留原音、时间戳与原始转写，不自动登记该段声纹；搜索和导出使用当前修正稿。
8. 本机搜索标题、正文或说话人，点击命中段落定位原声。支持导入 WAV、MP3、M4A，单个最多 64 MiB、30 分钟；完整保存后使用同一会后处理流程，没有虚构现场稿。

## 语音输入

按住可配置快捷键说话，松开后结束识别。开启「自动填入」时，统一通过临时剪贴板和一次 Cmd+V 尝试填入原位置；关闭后显示结果，手动复制。不自动按 Enter，也不因未确认而重复派发。

可以读取目标时核对原文、选区及结果；已知应用、窗口、目标或安全输入状态变化时停止填入。不可读取的目标不阻止录音；无法核验粘贴结果时保留文字并提示检查输入框。没有独立的微信兼容路径。目标检查与系统消费按键不是原子的，不保证任意输入框都可靠，也不用于密码等受保护输入。

粘贴后只在仍持有临时内容时恢复原剪贴板，保留用户期间的新复制。取消不会撤回已经派发的粘贴；恢复失败或状态未确认时保留相应提醒。详见隐私文档的剪贴板边界。

识别通过 DashScope 流式 ASR。文字润色是语音输入的可选步骤，默认关闭；失败保留可用原稿。结果与原稿保存在本机会库，音频不作为录会文件保存。录会与语音输入互斥。

## 常驻与设置

关闭主窗口时 Earshot 保留在菜单栏，快捷键继续可用。菜单提供打开会库、录制/保存、设置和退出；退出需要完成当前录音保存与导入清理。

个人热词一次保存供后续任务使用，支持的模型将词表随请求发送或使用已同步的云词表。设置页展示本机处理量、模型调用和参考费用；未知计量、未确认结束与实际余额不可读取要明确显示，估算不是云端账单。

## 隐私与授权

全部 BYOK Direct，没有 Earshot 账号、中继或 Credits。密钥由用户在设置输入，保存为本机 `0600` 明文文件。会话音频发送到云端识别，启用语音输入润色会发送文字；名字与轻量声纹本机保存。完整数据流见 [privacy.md](docs/privacy.md)。

系统权限按需授权，正常使用不每场重新确认。录会需要麦克风和屏幕/系统音频；语音输入需要麦克风，跨应用回填另需辅助功能。

## 非目标

会议纪要、会议稿 Chat 清稿、Agent、唤醒词、MCP、托管中继、本地 ASR、跨平台客户端、独立 daemon、VoiceProfileVault、灵动岛和桌面歌词。

## 依据

当前实现以 [implementation.md](docs/implementation.md) 和源码为准；[验收记录](docs/validation.md)区分已实现、已测试与真机未验证项。版本设计和历史评审保留为过程证据，不能直接当作当前产品承诺。
