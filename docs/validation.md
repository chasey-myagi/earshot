# 验证

## 自动检查

```sh
npm ci
npm run typecheck
npm test
npm run build
npm audit
```

测试以合成数据验证存储、录制生命周期、实时重连、异步任务恢复、回听、导出、命名、删除、语音输入状态机、快捷键设置和受保护的输入目标。OS 与云端边界使用替身，测试通过不等于真机通过。

## macOS 手动检查

| 功能 | 需要观察的结果 |
| --- | --- |
| 首次权限 | 麦克风、屏幕/系统音频与辅助功能授权状态准确，拒绝后有恢复入口 |
| 双轨录会 | 麦克风与已知系统声音实际进入对应 WAV，停录后可回听 |
| 实时与会后稿 | 录中有文字，断网不损坏音频，恢复后可继续或重跑 |
| 窗口与回听 | 关闭/切换窗口不丢录制控制，时间戳跳转对应原声 |
| 物理快捷键 | 按住收音、松开结束；切走输入目标后拒写，结果可复制 |
| 跨 App 回填 | TextEdit 等普通控件只替换选区，保留前后文字及 emoji |
| 本地安装 | 打包签名通过，安装后启动正常，旧会话与设置可读 |

以上是验收步骤，不是所有环境已经通过的声明。Intel Mac、全屏空间和外接显示器浮窗尚未验证。

## 2026-09-09：GitHub 0.1.0 预览版

- 发布构建将最低 macOS 统一为 26.4，并在打包前检查 native runtime 的最低目标；未替换当前已安装的本机 App。
- 本轮全量自动测试 696/696 通过、0 跳过，类型检查通过。最终提交的远端 Checks 和下载附件清单由对应 GitHub 标签、Actions 与 release-manifest.json 提供，历史 SHA 的绿灯不作为新包证明。
- 发布脚本只接受干净、已提交工作区，并记录完整 commit、锁文件哈希、运行时和模型版本、ZIP 哈希；模型与原生依赖从锁定来源准备。
- 本机无 Developer ID 分发证书，本版按开发签名、未公证预览分发。全新 Mac 的 Gatekeeper 首次打开、macOS 26.4 实机、外部 App 输入与先前手动矩阵仍未验证，不能视为已解决。

## 2026-09-09：当前本机版本

- 完整自动测试 695/695 通过、0 跳过，类型检查、打包和严格签名检查通过。此前 Unicode/微信路径测试已替换为通用剪贴板合同，数量与旧测试不可直接比较。
- 语音输入取消与恢复告警的最终独立代码复审通过（8.11/10）；代码审核不等于完整产品验收。
- 独立 Electron 合成 input、textarea、contenteditable 观察到选区替换和前后文保留，均一次粘贴、零 Enter；两类返回未确认，一类 AX verified，剪贴板恢复成功。实际 Electron Worker 与 named pasteboard 回归通过。
- 经用户最新指令，已替换本机 Applications 安装版并启动；历史会话和新版自动填入设置可见，三项权限显示已允许。旧 App 和会话数据已备份。此前“尚未安装”均是当时状态。
- 本轮未重新验收真实 Codex、微信、TextEdit、Terminal 输入，跨 Space、全屏、多屏浮窗，物理按住/松开与首词延迟，权限拒绝/撤销、真实慢剪贴板 provider 和远场声纹质量。设置显示允许不等于录音或回填通过。
- 当时发布预检发现包内 ONNX Runtime 的最低部署目标为 macOS 26.4，旧本机 App plist 声明 12.0；发布构建已修正为 26.4。仅在 macOS 26.7 / Apple Silicon 核对当前包，不承诺旧系统兼容。开发签名、未公证，GitHub 下载后的首次启动尚未验证。详见 [发布准备](release.md)。

## 历史验证记录

以下结果只对应各段日期及当时源码，不能覆盖当前版本或作为当前安装状态。

### 2026-09-07 基线

2026-09-07，Apple Silicon Mac，Electron 43.6.0：

- 433 项自动测试通过；类型检查、生产构建与严格签名校验通过。
- 真实双轨采集、已知系统声音的回听与定位校准通过；正式界面的实时转写、会后转写、说话人区分与失败重试通过。
- 设置页自定义快捷键保存通过；实际物理键盘按住、说话、松开并回填由操作者验收通过。
- 正式安装版已替换并正常启动，旧会话、设置和权限保留。跨 App 回填与目标变化后的拒写曾在 TextEdit 验证。

测试使用 OS 或云端替身时不计入上述真机结果。语音识别可能有误字；单次通过不代表任意音源或输入控件均受支持。Intel Mac、全屏空间、外接显示器浮窗以及无开发者证书的首次安装仍未验证。源代码构建不包含公证、自动更新或面向所有 Mac 的安装保证。


## 2026-09-08：转写恢复与声纹可靠性

本轮实现位于公开源码工作区，仍使用 Fun-ASR 与本机 CAM++，没有新增运行时或模型依赖。

- 持久化云任务与逐轨结果；自动恢复只续查或读取缓存，终态失败及未知提交需要明确人工重试，避免自动再次提交付费任务。
- 首轮代码审查与后续复审发现的五个状态/身份问题均有红灯复现和修复回归：部分结果回退、重试后的错误模板残留、旧分组污染登记、最后一次登记中断卡 running、失败云任务自动重提。
- 测试审查通过，七种登记状态通过实际 React 弹层静态渲染验证；浏览器预览中查看了 unavailable 提示、同名重试按钮及共用麦克风开关。
- 实际 Electron 43.6.0 utility worker 最初报 `External buffers are not allowed`。已使用当前 Sherpa 提供的 `compute(stream, false)` 请求复制缓冲区；重新打包后的 5 次 worker 调用成功，三份公开语音样例均返回 192 维有限向量，多片段证据返回 ready。
- 最终 512 项测试全数通过，无跳过；类型检查、生产包与严格签名校验通过。已更新 `/Applications/Earshot.app` 并确认旧会话可读；正式设置页的共用麦克风开关 off → on → off 验证通过。
- 公开样例拼接只验证模型/工作进程/多片段调用能跑通，不是会议准确率或阈值校准数据。

本轮未验证：新版本的真实会议云端转写、共享麦克风多人分组准确率、跨会姓名误认率，以及真机权限、系统音频、浮窗全屏行为。前述历史真机结果不自动代表本轮重新验收。

## 2026-09-08：全文工具、导入、设置与常驻升级（当时尚未安装）

本轮源码增加菜单栏常驻、段落修正/撤销/原稿、标题正文人物搜索、时间标记、倍速与前后 5 秒、WAV/MP3/M4A 导入、持久热词和本机用量估算。

- 中间完整回归 585/585 通过、无跳过，类型检查与生产构建通过。此数字对应微信兼容输入准备前的源码，不能自动代表后续修改。
- 实际主进程公共 IPC 覆盖导入取消、退出等待清理、原音与单轨元数据保存、修正/搜索/标记，以及菜单关闭/设置入口；录中书签遗漏与在途 Key/词表混用有先红后绿证据。
- 实际 Electron/Chromium 的 WAV、MP3、M4A 解码、损坏文件、30 分钟上限、取消清理和播放速率共 8 项通过。测试音频为生成的音调；ffmpeg 仅用于生成测试文件，不是应用运行依赖。
- 实际 React 在隔离 Chromium 中的 19 项 DOM 检查通过，包括热词后台换 Key 刷新、保留草稿、乱序响应、纠错撤销、搜索、标记、速率和跳转。使用合成 API，不能替代完整应用或真机输入验收。
- 7 项长文件测试通过，涵盖超过一小时与两小时 WAV 头修复、稀疏轨道 EOF、异常 JSONL 尾行和队列恢复；结合相关已有测试共 22 项通过。文件时长模拟不等于持续一小时麦克风/系统音频采集。
- [真实公共会议声纹评测](voiceprint-benchmark.md)已执行。修复了低增益人声被全部筛空的问题，独立近讲会次已有正确认回，但远场覆盖率仍不足；质量判定 `GAPS_FOUND`，未调低匹配门槛。

Mac 锁屏期间无法继续可见界面、微信/Codex 实际输入、物理快捷键或真机权限验收。离屏 DOM、测试替身、历史正式版的通过结果都不能代替本轮这些检查。**当前升级尚未替换 `/Applications/Earshot.app`；用户要求全部必要功能完成并逐项本地验收通过后才能替换。**

补充实测：隔离 `appData`/`userData` 的实际构建主进程、preload、React 与 Chromium 回听组合通过 24 项断言，覆盖搜索与原声定位、速率确认、修正侧文件与原稿保留、撤销、标记落盘、无 Key 热词、用量与窗口隐藏后继续响应。使用三条合成历史和静音 WAV，没有访问正式数据；这一检查仍不替代外部 App 光标回填。

### 本轮最终候选检查 · 2026-09-08

- 完整自动回归 **632/632 通过，0 跳过**，类型检查、生产构建和签名打包通过。包含直接输入成功不展开结果、失败保留可复制文字，以及同 Key 修改热词期间旧任务仍使用旧云词表内容的回归。
- 最终实际应用集成 **27/27 通过**，7 张离屏截图，已核对展开原稿、用量与余额说明、微信兼容默认关闭。仍使用隔离数据与合成录音，不等于锁屏下操作了真实外部 App。
- 包内真实 voiceprint worker 在 Electron utility process 完成 5 次推理调用，三条公开音频均得到有限的 192 维向量。同人余弦 0.591，高于异人 -0.082；这只是运行时冒烟，不是会议准确率证明。
- 微信兼容的生产桥 fake FFI、保护条件、状态机及渲染检查通过；原生 Unicode 内存探针验证 8192 UTF-16 单元往返，**实际投递 0 次**。真实微信输入、物理长按自动重复、输入法、停滞 AX 的响应仍未验证，保持默认关闭。
- 声纹公共会议独立评测仍为 **GAPS_FOUND**：近讲→远场 0/32 窗口认回，同远场登记的附加诊断为 9/32。不得以零错名替代认回覆盖率。1–2 小时稀疏文件/恢复测试通过，但真实连续一小时以上录音尚未验证。

候选仅保存在源码目录的 `dist/Earshot.app`。由于上述质量缺口和锁屏阻碍，全部功能本地验收门槛尚未满足，**没有替换已安装 App**。历史权限与用户口述通过记录不自动转移到该候选。

使用用户已保存的 Key 完成了两次真实百炼请求：Qwen Audio 3.0 和 Fun-ASR 文件识别各转写公开中文音频 5 秒，服务各返回 5 秒用量，隔离账本准确记为 2 请求/10 秒，公开原价估算 ¥0.00275（不是实际扣费）。两份本次创建的热词表均查询就绪并在结束后删除；Key 仅在内存，证据扫描未发现泄漏。Fun 实时模型的词表 API 已验证，但本轮未另外执行其流式识别或付费润色 Token 计量。余额仍不可由该推理 Key 查询。

## 2026-09-08 午间：实时恢复与改名补验

- 全量自动回归652/652通过；类型检查、生产构建、签名打包通过。每轨恢复六项固定契约先全部RED后GREEN，独立审核发现的限流分类错误亦有固定RED→GREEN证据；补齐29,999ms稳定窗口与两轨重试预算交错。
- 改名四入口统一布局，40项真实React DOM断言、19张浅深/窄窗口截图通过。之后补充逐轨断线原因与静态重试间隔，21项DOM断言和6项相关回归通过。
- 最后一次实际主进程/preload/React集成27/27通过。候选包全部out文件与构建逐一一致，严格codesign通过。代码和测试独立复审通过；不等于安装或真机验收授权。
- 用户43分钟会议的双轨WAV各含43分23.264秒PCM，现场稿代次证明两轮重连。旧版未记录原始错误原因，不能认定那两次断线由静音、网络或服务端中的哪一项造成。真实90秒纯静音对照两条连接均未断，结果为有界未复现。
- Mac曾解锁，后又锁屏。当前候选的TextEdit/Codex/微信实际回填、物理长按、菜单栏与录中浮窗仍未完整验收。一小时采集和45分钟云端观测的最终结果见下段。保留此前远场声纹质量缺口，仍未替换已安装App。

### 午间最终长时与信号检查 · 13:21

- 真实Fun-ASR双轨纯静音连接观察45分0.029秒，观察期间冻结的17项检查全部通过。仅第5分钟受控关闭麦克风轨，计划/实际退避均1秒，关闭请求后4.697秒收到新任务真实就绪；系统声音轨保留最初连接和任务ID，无自然失败事件。3个socket全部关闭，真实进程退出0，独立只读复算通过。
- 每轨生成2669.056秒静音PCM，尝试发送麦克风2668.160秒、系统轨2669.056秒。这些计数不是服务端接收或账单时长；不以0.896秒差值推断实际丢音。本测试没有上传麦克风/系统/会议音频，不证明旧会议断线根因或讲话准确率。
- 生产采集代码在开发Electron43.6.0身份下，真实连续采集双轨各3600.640秒PCM；两个WAV各115220524字节，16kHz/单声道/16bit，头与实际数据一致，权限0600。独立重数56260条帧元数据，与回调/文件样本完全一致；单调观察3601.02336秒、差0.38336秒满足运行期间冻结的合同。最大回调间隔约235/227ms，无大于1秒间隔；窗口、监听和活动handle清理完毕，进程退出0。
- 另一次本机afplay播放已知440→880Hz短纯音；独立限定读取system.wav中的8.704秒增量，检出按顺序分离的两段信号，证明该系统输出进入采集WAV。采集壳自身不生成音频，原始syntheticAudioPlayed=false字段不代表测试环境全程没有外部纯音。
- 上述采集使用隔离目录，不写正式会话，不读Key、不联网；仅证明该生产采集链路与时长，不替代候选App的TCC身份、完整UI、所有设备或语音保真验收。启动阶段有一条Chromium视频SharedImage ERROR级日志，早于首帧，未记录renderer crash。
- compact浮窗补充检查42/42、Library状态回归21/21通过，独立复审通过；默认380×300与最小320×220覆盖恢复状态、错误和存储警告。最拥挤尺寸保留51px可滚动文字区域，不宣称两行正文完全可见。

本轮完整软件与运行时证据已收齐，外部回填/物理快捷键/菜单栏及可见浮窗仍因桌面再次锁定而未完整验收，远场声纹质量缺口仍在。**已安装App保持原样，未越过全部验收门槛。**


## 2026-09-08：语音输入启动与多余滚动条（当时为未安装候选）

- 用户报告首次浮窗出现后立即说话容易漏首词，以及右侧多出滚动条。真实采集基线三次都出现 ready 早于首帧；首次从调用到首 PCM 约 912ms，ready 提前约 38ms。该测量不单独证明某个具体音素被截断。
- 修复后，以有效首帧作为启动完成条件，并保留异步调用返回前的首帧；启用时只预加载页面，按下快捷键后才请求麦克风。准备提示与“可以说话了”分开。
- 一次未预热采集为 1085ms；同进程后续三次预热采集为 149–177ms。另启新 Electron 进程，页面预热后的首次麦克风采集仍需 720ms。不能用后续预热数字宣称首次瞬时采音。预加载耗时单独记录，没有计入主动采集延迟。
- 所有原生探针只保留帧计数与时序，不读取密钥、不联网、不保存音频；空闲期无 PCM，采集页与监听器均清理完毕，各进程退出 0。真实 renderer 用例另验证页面加载不调用 getUserMedia。
- HUD 的 5px 外边距穿透满高根容器，导致 94px 窗口产生 99px 文档；使用局部 flow-root 包含外边距。固定 208 项布局合同先红后绿；更新实际提示文案后，32 个尺寸/主题/状态组合共 240 项通过，长结果仍可内部滚动。
- 完整自动回归 674/674 通过、0 跳过；类型检查、构建打包及 diff 检查通过。独立代码审查 8.49、测试审查 8.10 均通过当前切片。

实际按住快捷键后说话的首个音素、外部 App 回填，以及原升级的远场声纹质量仍需按各自验收边界确认；本轮不把状态/合成 PCM 用例当作实际首词通过，也没有替换 `/Applications/Earshot.app`。
